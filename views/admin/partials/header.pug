header.header 
    .ml-5.mr-5
        .row.align-items-center
            .col-3 
                .inner-logo 
                    a(href= `${prefixAdmin}/dashboard`) ADMIN 
            .col-9 
                div(class="text-right")
                    a(
                        href=`${prefixAdmin}/my-account`
                        class="btn btn-primary btn-sm mr-2"
                    ) #{user.fullName}
                    img(src=user.avatar, alt="", onclick="window.location.href='/admin/my-account'" ,class="avatar-user-mini-circle")
                    a(
                        href=`javascript: `
                        button-logout
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất 
