prefixAdmin
style.
    .admin-sider {
        background-color: #2c3e50;
        color: #ecf0f1;
        width: 280px;
        height: 100vh;
        padding: 20px 0;
        box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        z-index: 1000;
    }
    .sider-header {
        text-align: center;
        padding: 20px 0;
        border-bottom: 1px solid #34495e;
    }
    .sider-logo {
        width: 80px;
        height: 80px;
        margin-bottom: 10px;
    }
    .sider-title {
        font-size: 24px;
        margin: 0;
        font-weight: 300;
    }
    .sider-nav {
        padding-top: 30px;
    }
    .sider-menu {
        list-style-type: none;
        padding: 0;
        margin: 0;
    }
    .sider-menu-item {
        margin-bottom: 5px;
    }
    .sider-link {
        display: flex;
        align-items: center;
        padding: 12px 25px;
        color: #6c757d;
        text-decoration: none;
        transition: all 0.3s;
        border-left: 4px solid transparent;
    }
    .sider-link:hover, .sider-link.active {
        background-color: #34495e;
        border-left: 20px solid #3498db;
        border-left-color: #3498db;
        text-decoration: none;
    }
    .sider-link i {
        margin-right: 15px;
        width: 20px;
        text-align: center;
        font-size: 18px;
    }
    .sider-link span {
        font-size: 16px;
    }
    .sider-footer {
        position: absolute;
        bottom: 20px;
        width: 100%;
        text-align: center;
        font-size: 14px;
        color: #bdc3c7;
    }
    .dropdown {
        position: relative;
        display: inline-block;
    }
    .dropdown-menu {
        display: none;
        position: absolute;
        background-color: #34495e;
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        z-index: 1;
        left: 50%;
        transform: translateX(-50%);
        top: 100%;
        border-radius: 10px;    
    }
    .dropdown-menu li {
        padding: 12px 16px;
    }
    .dropdown-menu li:hover {
        background-color: #2c3e50;
    }
    .dropdown-menu a {
        color: #ecf0f1;
        text-decoration: none;
        display: block;
    }
    .show {
        display: block;
    }
.sider 
    .admin-sider
        .sider-header
            .dropdown
                img#avatarImg(src=user.Avatar, alt="" srcset="" style="border-radius: 40px;width: 200px;height: 200px;object-fit: cover;  margin: 20px; cursor: pointer;")
                ul#dropdownMenu.dropdown-menu
                    li
                        a(href="/admin/my-account") Tài khoản
                    li
                        a(onclick="window.location.href='/admin/my-account/edit'" style="cursor: pointer; color: white;") Chỉnh sửa
            h2.sider-title Quản trị viên
        nav.sider-nav
            ul.sider-menu
                li.sider-menu-item
                    a.sider-link(href=`${prefixAdmin}/dashboard`)
                        i.fas.fa-chart-area(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span Tổng quan
                li.sider-menu-item
                    a.sider-link(href=`${prefixAdmin}/account`)
                        i.fas.fa-users(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span Danh sách tài khoản
                li.sider-menu-item
                    a.sider-link(href=`${prefixAdmin}/service`)
                        i.fas.fa-fish(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span Dịch vụ
                li.sider-menu-item
                    a.sider-link(href=`${prefixAdmin}/setting/general`)
                        i.fas.fa-cogs.fa-3x.me-3(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span Cài đặt chung
                li.sider-menu-item
                    a.sider-link(href=`/admin/feedback`)
                        i.fa-solid.fa-star(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span FeedBack
                li.sider-menu-item
                    a.sider-link(href=`${prefixAdmin}/chat`)
                        i.fas.fa-comments.fa-3x.me-3(style="font-size: 40px;    margin-right: 40px;display: inline-block;")
                        span Chat 
script.
    document.addEventListener('DOMContentLoaded', function() {
        const avatarImg = document.getElementById('avatarImg');
        const dropdownMenu = document.getElementById('dropdownMenu');
        let isDropdownOpen = false;

        avatarImg.addEventListener('click', function(event) {
            event.preventDefault();
            event.stopPropagation();
            if (!isDropdownOpen) {
                dropdownMenu.classList.add('show');
                isDropdownOpen = true;
            } else {
                dropdownMenu.classList.remove('show');
                isDropdownOpen = false;
            }
        });

        window.addEventListener('click', function(event) {
            if (!event.target.matches('#avatarImg')) {
                if (dropdownMenu.classList.contains('show')) {
                    dropdownMenu.classList.remove('show');
                    isDropdownOpen = false;
                }
            }
        });
    });